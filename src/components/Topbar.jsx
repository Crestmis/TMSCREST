import {Bell,Search,Menu,LogOut} from 'lucide-react'
export default function Topbar({onMenu,session,onLogout}){return <header className="topbar"><button className="icon-btn mobile-only" onClick={onMenu}><Menu size={20}/></button><div className="top-search"><Search size={17}/><input placeholder="Search tasks, people or data..."/></div><div className="top-actions"><button className="icon-btn"><Bell size={18}/></button><div className="avatar">{session?.username?.slice(0,2).toUpperCase()}</div><div className="profile"><b>{session?.username}</b><small>{session?.role}</small></div><button className="icon-btn" title="Sign out" onClick={onLogout}><LogOut size={17}/></button></div></header>}
